import { BooleanToSiNoPipe } from './boolean-to-si-no.pipe';

describe('BooleanToSiNoPipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanToSiNoPipe();
    expect(pipe).toBeTruthy();
  });
});
